# The local app: the Streamlit UI, the ingestion mode, and the MCP server. This
# is what the Dockerfile installs, because one image serves all three modes.
# The deployed HTTP API installs requirements-api.txt instead and needs neither
# of these.
-r requirements-base.txt

streamlit>=1.40.0
# Official MCP Python SDK, pinned: the 2.x server API (MCPServer) differs from
# 1.x (FastMCP), so an unpinned upgrade would break mcp_server.py silently.
mcp==2.0.0
