# The deployed HTTP API, and only that. This is what Render installs, so it
# deliberately leaves out Streamlit and the MCP SDK: neither runs in the web
# service, and a smaller install is a faster cold start on a free instance.
-r requirements-base.txt

fastapi>=0.115.0
uvicorn>=0.30.0
